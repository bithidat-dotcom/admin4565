rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Standalone simplified ecommerce rules for real-time synchronization between storefront and admin
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
